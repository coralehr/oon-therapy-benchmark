"""v1 (Transparency-in-Coverage) test package.

Tests for the ``oon_bench`` v1 pipeline (schemas, aggregate, merge, query, api).
The package-level ``conftest.py`` puts the repo root on ``sys.path`` so
``import oon_bench`` and ``import therapy_codes`` resolve regardless of where
pytest is invoked from.
"""
